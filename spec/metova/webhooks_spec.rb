require 'spec_helper'

describe Metova::Webhooks do
  it 'has a version number' do
    expect(Metova::Webhooks::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
